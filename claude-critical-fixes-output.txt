All 14 critical fixes are complete. Here's a summary:

### Database fixes (SQL)
1. **RLS on `social_auth.tokens`** — Added `user_id` column, backfilled, enabled RLS with user + service_role policies
2. **RLS on `public.debug_logs`** — Enabled RLS, service_role-only policy
14. **Indexes** — Added `idx_posts_scheduled_lookup`, `idx_posts_user_id`, `idx_tokens_user_id`

### Code fixes
3. **GET /api/v2/accounts** — Added `.eq('user_id', user.id)` filter
4. **POST /api/v2/accounts/sync** — Safe column select, fixed `onConflict` to `'platform,account_id'`
5. **Cron secret** — Denies when `CRON_SECRET` is unset
6. **Open redirect** — Validates `next` param is a safe relative path
7. **Post pages** — Added server-side auth guards to both post routes
8. **Unauthenticated API routes** — Added auth to music, sfx, pexels, batch-export, proxy/media, chat
9. **Stale processing** — Posts stuck >10min are re-claimable
10. **Error sanitization** — All API routes return generic errors, real messages logged server-side
11. **Facebook logging** — Removed `tokenPrefix` and `tokenLength` from logs
12. **Account ownership** — Social media route verifies account belongs to user
13. **App metadata** — Title → "Combo", description → "AI-powered video editor"
