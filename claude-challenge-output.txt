Done. `UNIFIED_CALENDAR_PLAN.md` is written with:

**Critique summary:**
- **Plan A** built a feature palace on a broken foundation — never noticed the cron doesn't fire. Great UX ideas but massively over-scoped for zero revenue.
- **Plan B** nailed the #1 blocker (no cron trigger) and the schema bug, but over-engineered retry/monitoring for a system that hasn't published its first post. Zero UX improvements proposed.

**Unified plan cuts to 4 phases:**
- **Phase 0 (today):** Wire up `vercel.json` cron, fix admin client schema, add `processing_started_at` guard. Make posts actually publish.
- **Phase 1 (this week):** Quick-create from calendar, inline editor, reschedule button, platform color bands. Make the calendar actionable.
- **Phase 2 (next week):** Simple retry logic (3 retries, fixed delays), token refresh, failed post UI, compact pills, conditional drag-and-drop.
- **Phase 3 (later):** Recurring schedules, bulk ops, audit logs, rate limits — only when users demand them.

Everything that doesn't serve a product with zero paying users got cut: schedule templates, calendar sidebar, calendar events table, `publish_log`, rate limit tracking, account group swimlanes, exponential backoff formulas.
