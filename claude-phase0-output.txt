All 6 tasks complete. Here's a summary of what was done:

| Task | What changed |
|------|-------------|
| **0.1** | `admin.ts` — Added optional `DbSchema` param so the admin client can target `social_auth` schema |
| **0.2** | DB migration — Added `processing_started_at` (timestamptz) and `retry_count` (integer, default 0) columns to `social_auth.posts` |
| **0.3** | `cron/publish-scheduled/route.ts` — Uses `createAdminClient('social_auth')`, filters with `.is('processing_started_at', null)`, sets `processing_started_at` before processing and clears it on completion |
| **0.4** | `editor/vercel.json` — Created with every-minute cron schedule for `/api/cron/publish-scheduled` |
| **0.5** | `calendar-content.tsx` — Added red banner when failed posts exist, with "Show failed" button to filter |
| **0.6** | Type check passed — all errors are pre-existing, none from these changes |
